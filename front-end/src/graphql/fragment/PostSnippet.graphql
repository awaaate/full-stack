fragment PostSnippet on Post {
    id
    slug
    title
    updatedAt
    createdAt
    points
    voteStatus
    topics {
        id
        name
    }
    creator {
        id
        username
    }
}
