query Post($id: Int!) {
    post(id: $id) {
        id
        slug
        title
        content
        updatedAt
        createdAt
        points
        voteStatus
        topics {
            id
            name
        }
        creator {
            id
            username
        }
    }
}
