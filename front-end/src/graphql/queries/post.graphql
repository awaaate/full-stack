query Post($id: Int!) {
    post(id: $id) {
        id
        title
        text
        creatorId
        updatedAt
        createdAt
        points
        voteStatus
        creator {
            id
            username
        }
    }
}
